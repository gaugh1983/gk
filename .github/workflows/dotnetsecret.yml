name: .NET Secret

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Environment Secrets
      run: |
        if [ "$TEST_SECURE" == "DEV AWS KEYS"
        then 
          echo "Got Dev AWS Keys"
        fi
      env:
       Test_SECURE: ${{ secrets.DEVAWSKEYS }}
  
